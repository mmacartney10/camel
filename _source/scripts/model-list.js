(function(window, undefined) {

  function showModelList(modelList) {

  }

  function init() {
    // socket.emit('client:getModelList', '');
    // socket.on('server:sendModelList', showModelList);
  }

  init();

})(window);
